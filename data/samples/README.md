# Sample EPUBs

Git-only fixtures for M1+. Excluded from `meson dist` (see `.gitattributes`).

| File | Role |
|---|---|
| `kafka-metamorphosis.epub` | Fiction (short novel) |
| `williams-free_as_in_freedom.epub` | Tech (Williams, *Free as in Freedom*) |
| `astounding-1933-03-jack-williamson.epub` | Sci-fi periodical (*Astounding Stories*, March 1933) |

Copied from `~/Books`. Not installed with the `.deb`.
